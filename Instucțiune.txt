Download Dataset File

# Get presigned URL and download file
RESPONSE=$(curl -X POST "https://mozilladatacollective.com/api/datasets/cmn2e8rmi01l6mm07vxurptse/download" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json")

# Extract download URL and download file
DOWNLOAD_URL=$(echo $RESPONSE | jq -r '.downloadUrl')
curl -o "Common Voice Scripted Speech 25.0 - Romanian.tar.gz" "$DOWNLOAD_URL"

Usage Instructions:

Replace YOUR_API_KEY with your actual API key in both commands
Run the create download session command to get a download token
Dataset ID
cmn2e8rmi01l6mm07vxurptse

Dataset Slug
common-voice-scripted-speech-25-0-romani-701de4ae